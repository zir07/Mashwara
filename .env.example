# Database Configuration
POSTGRES_USER=mashwara
POSTGRES_PASSWORD=mashwara_dev_password
POSTGRES_DB=mashwara_db
DATABASE_URL="postgresql://mashwara:mashwara_dev_password@localhost:5432/mashwara_db?schema=public"

# pgAdmin Configuration (Optional)
PGADMIN_EMAIL=admin@mashwara.local
PGADMIN_PASSWORD=admin

# API Configuration
API_PORT=3000
NODE_ENV=development

# JWT Configuration (for future auth implementation)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
